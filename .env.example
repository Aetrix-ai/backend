# Example environment variables for this project
# Copy this file to `.env` and update values before running the app.
# Do NOT commit your real secrets. This file contains safe example values.

#############################
# App / Environment
#############################
# NODE_ENV controls behavior in `src/config.ts` (development | production)
NODE_ENV=development

#############################
# Server
#############################
# Port the server listens on
PORT=3000

#############################
# Database (Prisma)
#############################
# Example PostgreSQL connection string. Replace user, password, host, port and dbname as needed.
# For production, use a secure connection string and do not check it into source control.
DATABASE_URL=postgresql://postgres:password@localhost:5432/backend_db?schema=public

#############################
# JWT / Authentication
#############################
# Secrets used to sign JWTs. Replace with long, random strings in production.
USER_JWT_SECRET=replace_with_a_strong_random_value_for_user_jwt
ADMIN_JWT_SECRET=replace_with_a_strong_random_value_for_admin_jwt

#############################
# AI Provider
#############################
# API key for OpenAI or whichever AI provider is used. Keep secret.
OPENAI_API_KEY=sk-xxxxxx
# Provider name used by the app (default: openai)
AI_PROVIDER=openai
# Model sampling temperature (0.0 - 1.0). Default: 0.7
AI_TEMPERATURE=0.7
# Max tokens to request from the AI provider (integer)
AI_MAX_TOKENS=1500

#############################
# Security / Hashing
#############################
# bcrypt salt rounds (integer). Reasonable defaults: 10 for production, lower for tests.
BCRYPT_SALT_ROUNDS=10

#############################
# Admin Initialization
#############################
# If set to "true", the app will expect SUPERADMIN_EMAIL and SUPERADMIN_PASSWORD to create an initial admin.
INIT_ADMIN=false
SUPERADMIN_EMAIL=admin@example.com
SUPERADMIN_PASSWORD=ChangeMe123!

#############################
# Logging
#############################
# pino log level: trace | debug | info | warn | error | fatal
LOG_LEVEL=info

#############################
# Notes
#############################
# - Keep real secrets out of source control. Use environment-specific secret stores or CI/CD secret management.
# - In production:
#     * Set NODE_ENV=production
#     * Use a production-grade DATABASE_URL (postgresql://...)
#     * Replace JWT secrets with cryptographically secure random strings
#     * Do not enable INIT_ADMIN unless you intend to create a superadmin and have secure SUPERADMIN_PASSWORD
# - The app's `src/config.ts` contains runtime validation that will throw errors if crucial values are missing in production.
